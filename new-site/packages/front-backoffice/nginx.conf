server {
    listen 80;

    location = / {
        return 301 /admin/;
    }

    location /admin/ {
        root /usr/share/nginx/html;
        index index.html;
        
        try_files $uri $uri/ /admin/index.html;
    }
}